import { AnswerPresetGuard } from './answer-preset.guard';

describe('AnswerPresetGuard', () => {
  it('should be defined', () => {
    expect(new AnswerPresetGuard()).toBeDefined();
  });
});
